// Este archivo define la ruta `/register` y renderiza la primera pantalla del flujo de registro.
import RegisterCredentialsScreen from '../screens/RegisterCredentialsScreen';

export default RegisterCredentialsScreen;

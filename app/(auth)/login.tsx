// --- Explicación del archivo ---
// Este archivo actúa como el punto de entrada para la ruta '/login'.
// Su única responsabilidad es importar y exportar el componente de la pantalla de login.
// Expo Router escanea el directorio `app` y crea una ruta por cada archivo.
// Así, `app/login.tsx` se convierte en la URL `http://localhost:8081/login`.

import LoginScreen from '../../screens/LoginScreen';

export default LoginScreen;
